PLANE_BASE_URL=https://api.plane.so
# Optional fallback if a user has no workspace_slug stored
PLANE_WORKSPACE_SLUG=fs

DB_HOST=localhost
DB_PORT=5432
DB_NAME=plane
DB_USER=plane_user
DB_PASSWORD=replace_me
DB_SCHEMA=mcp
